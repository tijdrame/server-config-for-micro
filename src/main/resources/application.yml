spring:
  application:
    name: CONGIG-SERVER
  cloud:
    config:
      server:
        git:
          url:

#management:
#  endpoints:
#    web:
#      exposure:
#        include: "*"

server:
  port: 8888

eureka:
  instance:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

